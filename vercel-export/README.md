# Dirt Bike Dash

Vercel setup:

1. Upload or import this folder as a project.
2. Framework preset: `Vite`.
3. Build command: `npm run build`.
4. Output directory: `dist`.

The game saves leaderboard scores and bike upgrades in each browser's local storage.
